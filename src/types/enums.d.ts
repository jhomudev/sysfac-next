export enum EUserType {
  admin = 'Admin',
  superadmin = 'SuperAdmin',
  seller = 'Vendedor'
}

export enum EUserState {
  active = 'Activo',
  inactive = 'Inactivo'
}

export enum ESaleFor {
  quantity = 'Cantidad',
  unit = 'Unidad/NS'
}

export enum ELocationType {
  warehouse = 'Almacén',
  store = 'Tienda'
}

export enum EStateProduct {
  active = 'Activo',
  inactive = 'Inactivo'
}
export enum EStateProductUnit {
  stock = 'En stock',
  sold = 'Vendido',
  damaged = 'Dañado'
}

export enum EOperationType {
  buy = 'Compra',
  sell = 'Venta'
}

export enum EProofType {
  invoice = 'Factura',
  ticket = 'Boleta de venta'
}
